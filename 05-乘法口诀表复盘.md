# 习题5：9×9乘法口诀表 错题复盘报告

## 题目回顾

输出下三角九九乘法表。

```
1*1=1  1*2=2  2*2=4  1*3=3  2*3=6  3*3=9  ……
1*9=9  2*9=18 ………… 9*9=81
```

要求：
1. 使用双层 for 循环嵌套；外层控制行，内层控制列。
2. 每一行结束之后换行。
3. 表达式格式：`i*j=xx`，j 代表当前行数，i 代表列数。

## 你的原始代码

```c
#include <stdio.h>
int main()
{int i,j;
for(j=1;j<10;j++)
{
    for(i=1;i<=j;i++)
    {
        printf("%d*%d=%2d ",i,j,i*j);
    }
    printf("\n");
}
    return 0;
}
```

## ✅ 正确点

- 双层循环边界正确：外层 j（行）1~9；内层 i（列）i<=j，实现下三角。
- `%2d` 格式化输出，乘积占 2 字符宽度，排版对齐。
- 内层循环结束执行换行，每行输出结束自动换行。

## 小瑕疵

- 每一行输出结尾会多出一个多余空格，考试部分严格判卷会扣分。

## ✅ 优化版本（去除每行末尾多余空格）

```c
#include <stdio.h>
int main()
{
    int i,j;
    for(j=1;j<10;j++)
    {
        for(i=1;i<=j;i++)
        {
            printf("%d*%d=%2d",i,j,i*j);
            if(i < j) //不是本行最后一列，才输出分隔空格
            {
                printf(" ");
            }
        }
        printf("\n");
    }
    return 0;
}
```

## 📚 引申知识点

### 1. 双层嵌套循环做题套路

外层循环：控制行；内层循环：控制本行内部每一列。
九九表核心：第 j 行，就有 j 列，所以内层条件 `i <= j`。

### 2. printf 格式化输出

`%2d`：将整数输出占 2 个字符宽度，数字右对齐，用来对齐乘法表。

### 3. 消除末尾多余空格技巧

循环打印一组数据时：先打印内容，判断是否不是最后一个元素，如果不是，输出分隔符（空格）。

```c
if(i < j)
{
    printf(" ");
}
```

## 📝 测试输出

```
1*1= 1 1*2= 2 2*2= 4 1*3= 3 2*3= 6 3*3= 9 1*4= 4 2*4= 8 3*4=12 4*4=16 1*5= 5 2*5=10 3*5=15 4*5=20 5*5=25 1*6= 6 2*6=12 3*6=18 4*6=24 5*6=30 6*6=36 1*7= 7 2*7=14 3*7=21 4*7=28 5*7=35 6*7=42 7*7=49 1*8= 8 2*8=16 3*8=24 4*8=32 5*8=40 6*8=48 7*8=56 8*8=64 1*9= 9 2*9=18 3*9=27 4*9=36 5*9=45 6*9=54 7*9=63 8*9=72 9*9=81
```

## 踩坑总结

下三角九九表关键：内层循环的终止条件依赖外层循环变量。
打印分隔空格：不要每次打印完都无脑加空格；判断不是最后一项再加空格，避免行尾多余空格。
