# 习题4：完数（真因数求和）错题复盘报告

## 题目回顾

输入一个正整数 n（2 ≤ n ≤ 1000），求它所有**真因数之和**。
真因数：不包含数字本身的约数。
示例：n=6，真因数是 1、2、3，和为 6。

要求：
1. 如果因数和等于 n，输出：`是完数`，否则输出：`不是完数`
2. 如果输入小于 2 或者大于 1000，提示：`输入数据非法`
3. 使用 ret 接收 scanf 返回值，拦截字母等非法输入。

## 你的原始代码

```c
#include <stdio.h>
int main()
{
    int n,ret;
    int i=1;
    int sum=0;
    printf("请输入一个正整数n:（2<=n<=1000)\n");
    ret=scanf("%d",&n);
    if(ret!=1)
    {
        printf("输入数据失败\n");
        return 1;
    }
    if(n<2)
    {
        printf("输入数据非法\n");
    }
    for(i=1;i<n;i++)
    {if(n%i==0)
    {
        sum+=i;
    }
    }
    if(n==sum)
    {
        printf("%d是完数\n",n);
    }
    else printf("%d不是完数\n",n);
    return 0;
}
```

## ❌ 错误 & 缺陷清单

1. **输入非法 (n<2) 之后没有退出程序**：仅仅打印提示字符串，没有 `return 1;`，程序继续执行后面循环与判断，会额外输出一行完数判断结果，造成输出错误。
2. **缺少上限判断**：题目要求范围 2~1000，没有处理 n>1000 的情况。

## ✅ 做对部分

- 使用 ret 接收 scanf 返回值，处理字母等非法输入。
- sum 初始化为 0；循环条件 i<n，正确筛选真因数（排除自身）。
- 完数判断条件 n == sum 逻辑正确。

## ✅ 完整优化后代码

```c
#include <stdio.h>
int main()
{
    int n,ret;
    int sum=0;
    printf("请输入一个正整数n:（2<=n<=1000)\n");
    ret=scanf("%d",&n);
    if(ret!=1)
    {
        printf("输入数据失败\n");
        return 1;
    }
    if(n < 2 || n > 1000)
    {
        printf("输入数据非法\n");
        return 1;
    }

    for(int i=1;i<n;i++)
    {
        if(n%i==0)
        {
            sum+=i;
        }
    }
    if(n==sum)
    {
        printf("%d是完数\n",n);
    }
    else
    {
        printf("%d不是完数\n",n);
    }
    return 0;
}
```

## 📚 引申知识点

### 1. 真因数求解

真因数：约数，不包含数字本身。循环条件 `i < n`。
`if(n%i == 0)` 代表 i 是 n 的约数。

### 2. 边界输入处理套路

当输入不满足合法范围，打印提示并且 `return 1` 结束程序，不能只打印文字继续往下跑。

```c
if(条件非法)
{
    提示;
    return 1;
}
```

### 3. 变量定义

C 语言支持在 for 循环头内部定义循环变量 `for(int i=1; i<n; i++)`，减少局部变量声明，代码更整洁。

## 📝 测试用例

| 输入 | 输出 |
|---|---|
| 6 | 6是完数 |
| 12 | 12不是完数 |
| 1 | 输入数据非法 |
| 2000 | 输入数据非法 |

## 踩坑总结

边界条件判断，输出警告之后，一定要终止程序，防止后续逻辑继续执行产生多余输出。
