# 习题三：水仙花数 错题复盘报告

## 题目回顾

找出 100 ~ 999 之间所有水仙花数。
水仙花数：三位数，各位数字立方之和等于自身。
例：`153 = 1³ + 5³ + 3³`，空格隔开输出全部水仙花数。

## 你的原始代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int n;
    int ge,shi,bai;
    for(n=100;n<1000;n++)
    {
        ge=n%10;
        n/=10;
        shi=n%10;
        bai=n/10;
        if(n==ge*ge*ge+shi*shi*shi+bai*bai*bai)
        {
            printf("%d ",n);
        }
    }
    printf("\n");
    return 0;
}
```

## ❌ 错误清单

1. **for 循环内部修改循环变量 n（致命 bug）**：`n /= 10;` 直接修改 for 的控制变量 n，破坏循环迭代。for 循环每轮末尾还会执行 n++，n 数值彻底错乱，循环逻辑完全失效。
   原则：for 循环中，尽量不要修改循环变量；如果要做运算，复制到临时变量操作。
2. **多余头文件**：`#include <stdlib.h>`、`#include <math.h>` 本题没有使用，冗余，应当删除。

## ✅ 做对部分

- 循环范围 100-999 范围正确。
- %10 取个位，/10 去掉个位的数位拆分思路是正确的。
- 判断条件公式逻辑正确。

## ✅ 完整优化后代码

```c
#include <stdio.h>
int main()
{
    int n;
    int ge, shi, bai;
    int temp;

    for(n = 100; n < 1000; n++)
    {
        temp = n;       //拷贝到临时变量，原始n保持不变
        ge = temp % 10;
        temp /= 10;
        shi = temp % 10;
        bai = temp / 10;

        if(n == ge*ge*ge + shi*shi*shi + bai*bai*bai)
        {
            printf("%d ", n);
        }
    }
    printf("\n");
    return 0;
}
```

## 📚 引申知识点

### 1. 三位数数位拆分模板（软考高频）

```c
temp = n;
ge = temp % 10;   //个位
temp = temp / 10;
shi = temp % 10;  //十位
bai = temp / 10;  //百位
```

一定要用临时变量 temp，保护原始数字 n。

### 2. for 循环注意点

```c
for(初始化; 条件; 增量)
{
    //不要随便修改这里的循环变量！
}
```

如果在循环体内修改循环变量，会打乱 n++ 的自增逻辑，出现漏循环、死循环、提前退出。

### 3. 整数立方

直接 `ge*ge*ge`，不要用 pow 函数；pow 返回 double，容易产生浮点数精度误差。

## 📝 测试输出

输出：

```
153 370 371 407
```

## 踩坑总结

数位拆分，复制一份给临时变量去撕扯，原始数字保留用来做判断。
