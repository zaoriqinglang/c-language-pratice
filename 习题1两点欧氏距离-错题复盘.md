# 习题1：两点欧氏距离 错题复盘报告

## 一、题目回顾

输入 4 个 double 型浮点数坐标 `x1 y1 x2 y2`，计算两点欧氏距离，保留 3 位小数输出，处理输入读取失败。

## 二、你的原始代码（最初版本）

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    double x1,x2,y1,y2;
    double p,q;
    double distance;
    printf("请输入第一组坐标\n");
    scanf("%d%d",&x1,&y1);
    printf("请输入第二组坐标\n");
    scanf("%d%d",&x2,&y2);
    p=x2-x1;
    q=y2-y1;
    distance=sqrt(p*p+q*q);
    printf("两点之间的欧氏距离是:%d\n",distance);
    return 0;
}
```

## 三、你的第二版代码（自己修改格式符之后，仍存在缺陷）

```c
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    double x1,x2,y1,y2;
    double p,q;
    double distance;
    printf("请输入第一组坐标\n");
    scanf("%lf%lf",&x1,&y1);
    printf("请输入第二组坐标\n");
    scanf("%lf%lf",&x2,&y2);
    p=x2-x1;
    q=y2-y1;
    distance=sqrt(p*p+q*q);
    printf("两点之间的欧氏距离是:%.3f\n",distance);
    return 0;
}
```

## 四、❌ 错误清单

1. **格式符匹配错误（第一版致命错误）**
   - double 变量，scanf 误用 `%d`，`%d` 只用于 int，读取得到乱码垃圾值。
   - printf 输出 double 误用 `%d`，整数格式打印浮点数，输出完全错误。
2. **多余无用头文件**：`#include <stdlib.h>` 本程序完全没有使用，属于冗余代码，应当删除。
3. **缺少输入合法性校验（题目硬性要求）**：没有接收 scanf 返回值，没有处理用户输入字母、符号、数字个数不足的场景。一旦非法输入，变量保存随机垃圾值，继续运算输出错误结果。

## 五、✅ 做对的部分

- 变量使用 double 类型；
- 数学公式逻辑正确，`sqrt()` 开平方运算使用思路正确；
- 第二版已经修正：`scanf("%lf")` 读取 double、`printf("%.3f")` 保留 3 位小数。

## 六、✅ 完整优化后代码

```c
#include <stdio.h>
#include <math.h>

int main()
{
    double x1,x2,y1,y2;
    double p,q;
    double distance;
    int ret;

    printf("请输入第一组坐标\n");
    ret = scanf("%lf%lf",&x1,&y1);
    if(ret != 2)
    {
        printf("输入读取失败！\n");
        return 1;
    }

    printf("请输入第二组坐标\n");
    ret = scanf("%lf%lf",&x2,&y2);
    if(ret != 2)
    {
        printf("输入读取失败！\n");
        return 1;
    }

    p = x2 - x1;
    q = y2 - y1;
    distance = sqrt(p*p + q*q);
    printf("两点之间的欧氏距离是:%.3f\n",distance);
    return 0;
}
```

## 七、📚 引申知识点（软考高频考点，必须背）

### 1. int 与 double 格式符对照表

| 数据类型 | scanf 输入格式符 | printf 输出格式符 |
|---|---|---|
| int | `%d` | `%d` |
| double | `%lf` | `%f` / `%.3f` |

**重点坑：**
1. scanf 读取 double **必须写 `%lf`**；
2. printf 打印 double **只用 `%f`**，不要写 `%lf`；编译不会报错，但运行结果错乱，属于软考选择题高频坑。

### 2. scanf 返回值 —— ret 变量作用

```c
ret = scanf(格式串, 变量列表);
```

返回值含义：本次调用**成功读取到的变量数量**。

示例：`scanf("%lf%lf", &x1, &y1);` 预期读取 2 个浮点数

- 用户输入合法数字 → ret = 2
- 只读到 1 个有效数字 → ret = 1
- 输入字母符号，一个都读失败 → ret = 0

业务写法：

```c
if(ret != 预期读取数量)
{
    提示输入错误;
    return 1;   // 异常退出
}
```

### 3. main 函数返回值含义

- `return 0;`：程序正常执行完毕，正常退出。
- `return 1;`（非 0）：程序异常、出错退出。

### 4. math.h 库函数

- `sqrt(参数)`：计算平方根，参数必须浮点类型；
- 编译注意：gcc 命令行编译需要加 **`-lm`** 链接数学库；Dev-C++ IDE 不需要手动加。

### 5. 代码规范要点

- 没有使用的头文件，一定要删除，不要无脑复制粘贴头文件。

## 八、📝 测试用例

**输入：**

```
0 0 3 4
```

**输出：**

```
两点之间的欧氏距离是:5.000
```

## 九、本次踩坑总结

很多代码编译可以通过，但是运行结果错误，绝大多数都是格式符与变量类型不匹配。写代码写完第一时间检查：**变量类型、scanf/printf 格式符是否一一对应**。
