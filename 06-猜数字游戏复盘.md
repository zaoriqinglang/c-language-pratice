# 习题6：简易猜数字游戏 错题复盘报告

## 题目回顾

1. 固定秘密数字 secret = 66
2. 用户循环输入猜测数字
3. 猜大输出：`大了，再试试`
4. 猜小输出：`小了，再试试`
5. 猜对退出循环，打印恭喜，输出猜测总次数。
6. 使用 ret 接收 scanf 返回值，非法输入直接退出。

拓展练习：使用 rand/srand 实现 1-100 随机秘密数字。

## 你的原始代码（随机拓展版）

```c
#include <stdio.h>
#include <time.h>
int main()
{
    srand((unsigned)time(NULL));
    int secret = rand()%100 + 1; //1~100随机秘密数字
    int guess,ret;
    int count = 0;
    while(1)
    {printf("请输入数字\n");
        ret=scanf("%d",&guess);
        if(ret!=1)
        {
            printf("输入错误!\n");
            return 1;
        }
        if(guess==secret)
        {
            count++;
            printf("第%d次猜中\n",count);
            break;
        }
        else if(guess<secret)
        {
            printf("猜小了\n");
            count++;
        }
        else {
            printf("猜大了\n");
            count++;
        }
    }
    return 0;
}
```

## ❌ 缺陷清单

- **缺失头文件 `#include <stdlib.h>`**：srand() 与 rand() 函数需要这个头文件。部分编译器可以兼容编译，标准 GCC、VS 会报错。

## ✅ 做对部分

- srand((unsigned)time(NULL)) 写在 main 开头，没有放入循环，随机种子使用规范。
- ret 校验 scanf 返回值，拦截字母非法输入。
- while(1) 死循环 + break 退出游戏逻辑正确。
- count 计数器每次有效猜测计数 + 1，计数逻辑准确。

## ✅ 完整修正代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main()
{
    srand((unsigned)time(NULL));
    int secret = rand()%100 + 1; //1~100随机秘密数字
    int guess,ret;
    int count = 0;
    while(1)
    {
        printf("请输入数字\n");
        ret=scanf("%d",&guess);
        if(ret!=1)
        {
            printf("输入错误!\n");
            return 1;
        }
        if(guess==secret)
        {
            count++;
            printf("第%d次猜中\n",count);
            break;
        }
        else if(guess<secret)
        {
            printf("猜小了\n");
            count++;
        }
        else
        {
            printf("猜大了\n");
            count++;
        }
    }
    return 0;
}
```

## 📚 引申知识点

### 1. rand 随机数模板

头文件：

```c
#include <stdlib.h>
#include <time.h>
```

main 开头只调用一次：

```c
srand((unsigned)time(NULL));
```

生成 [min, max] 区间整数：

```c
int num = rand() % (max - min + 1) + min;
```

严禁把 srand() 写到循环内部，会造成随机失效。

### 2. 循环输入游戏固定套路

- scanf 读取 + ret 校验
- 计数器 ++ 统计次数
- 多分支比较
- 猜对 break 跳出循环

## 📝 测试样例

```
请输入数字
30
猜小了
请输入数字
70
猜大了
请输入数字
50
猜小了
请输入数字
55
第4次猜中
```

## 踩坑总结

rand、srand 必须带 stdlib.h 头文件；srand 只初始化一次，不要写循环里面。
