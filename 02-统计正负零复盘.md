# 习题2：统计正数、负数、零 错题复盘报告

## 题目回顾

不断输入整数，输入数字 9999 结束程序。程序需要统计：

- 正数的个数
- 负数的个数
- 0 的个数

终止数字 9999 不要参与统计。

## 你的原始代码

```c
#include <stdio.h>
int main()
{
    int n;
    int count1=0;//统计正数的个数（不包含9999）
    int count2=0;//统计负数的个数
    int count3=0;//统计零的个数
    while(1)
    {
        scanf("%d",&n);
        if(n>0&&n!=9999) count1++;
        else if(n<0) count2++;
        else if(n==0) count3++;
        else
        {
            break;//当输入9999立即终止循环
        }
    }
    printf("正数个数（不包含9999）：%d 负数个数：%d 零个数：%d \n",count1,count2,count3);
    return 0;
}
```

## ❌ 错误 & 缺陷清单

1. **没有 scanf 返回值校验**：用户输入字母、符号，读取失败，程序直接死循环。软考上机题目，非法输入处理是得分点。
2. **多余条件判断**：`if(n>0&&n!=9999)`，9999 大于 0，但 9999 会直接走到 else 分支 break，永远不会进入这个 if，`n!=9999` 属于多余判断，不影响运行，但代码冗余。

## ✅ 做对部分

- 使用 while(1)+break 实现循环输入，终止标记 9999 不会计入统计。
- 计数器初始化全部为 0。
- 正数、负数、0 分支逻辑正确。

## ✅ 完整优化后代码

```c
#include <stdio.h>
int main()
{
    int n;
    int ret;
    int count1=0;//正数
    int count2=0;//负数
    int count3=0;//零

    while(1)
    {
        ret = scanf("%d",&n);
        //读取失败直接退出
        if(ret != 1)
        {
            printf("输入读取失败！\n");
            return 1;
        }

        if(n == 9999)
        {
            break; //遇到终止符直接退出，不参与统计
        }

        if(n>0)
            count1++;
        else if(n<0)
            count2++;
        else
            count3++;
    }
    printf("正数个数：%d 负数个数：%d 零个数：%d \n",count1,count2,count3);
    return 0;
}
```

## 📚 引申知识点

### 1. while(1) 无限循环模式

while(1) 代表条件永远成立，循环不会自动停止，依靠内部 break 跳出循环。
做题套路：优先判断结束标记，遇到结束标记直接 break，再做统计逻辑，可读性最好。

### 2. scanf 返回值

```c
ret = scanf("%d",&n);
```

预期读取 1 个整数

- ret == 1：读取成功
- ret != 1：读取失败（输入字母符号），程序异常退出。

### 3. 计数器规范

统计用变量，循环开始前必须初始化为 0，如果忘记初始化，里面是随机垃圾值。

## 📝 测试用例

**输入：**

```
12 -5 0 33 0 -1 9999
```

**输出：**

```
正数个数：2 负数个数：2 零个数：2
```

## 踩坑总结

业务逻辑跑通不等于满分，软考上机会考察异常输入；遇到循环输入题目，一定要思考：用户乱输入字母怎么办。
